# This file is part of the standard testthat directory structure.
# It is automatically run by `R CMD check` and by `devtools::test()`.

library(testthat)
library(dartScoreR)

test_check("dartScoreR")
